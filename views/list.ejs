<%- include("header") -%>

<div class="container" id="content">
  <div class="heading">
    <h2>
      <%= title %> List
    </h2>
  </div>

  <div class="items">
    <%  for( let item of newListItems){ %>
<form action="/delete" method="post">
  <div class="checkbox">
    <label class="container">
      <input type="checkbox" class="item-checkbox m-2" value="<%= item._id %>" name="checkbox" onChange="this.form.submit()">
      <span class="item-name">
        <%= item.name %>
      </span>
    </label>
  </div>
  <input type="hidden" name="listName" value="<%= title %>"></input>
</form>
    <% } %>
    <form action="/" method="post">
      <div class="form-inline" id="add-item">
        <input type="text" class="form-control m-3" name="newItem" placeholder="Add new item">
        <button class="btn btn-info" type="submit" name="list" value="<%= title %>">Add</button>
      </div>
    </form>

  </div>

</div>

<%- include("footer") -%>
